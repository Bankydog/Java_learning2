const a = [
  {
    pets: ["cat", "dog"], // Use an array for multiple values
    colors: ["green", "blue", "red"], // Use an array for multiple values
  },
  22, // Integer in the array
  "text", // String in the array
];
console.log(a[0].pets[0]);

const b = { number: [1, 2, 3, 4] };
console.log(b.number[2]);
